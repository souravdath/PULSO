name: pulso_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Backend
  supabase_flutter: ^2.10.3
  google_sign_in: ^6.2.2  # Kept from your friend's update

  # Navigation
  go_router: ^14.0.0

  # State Management
  provider: ^6.1.1

  # Bluetooth & Permissions
  permission_handler: ^11.3.0
  flutter_bluetooth_serial: ^0.4.0

  # UI & Charts
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  google_generative_ai: ^0.4.7
  
  # Image Capture & Storage
  screenshot: ^3.0.0
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true